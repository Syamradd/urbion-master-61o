# MASTER-142 — Next Attacks

After MASTER-141 CI passes, attack the actual product surfaces rather than adding release paperwork:

- verify every dashboard command-centre link resolves to a real page;
- implement or remove any advertised Map Studio controls that are not actually interactive;
- expose evidence state and source provenance at the point of decision;
- make What-If outputs visually comparable with baseline vs scenario deltas;
- make Decision Center reachable from the primary dashboard journey;
- expose KM readiness as a planner-review action, without implying approval;
- exercise malformed, missing, non-finite and unsupported spatial inputs through red-team tests;
- verify live deployment identity against the current main release.

Success means visible, testable, explainable functionality — not merely more endpoints or documentation.
