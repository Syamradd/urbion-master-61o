# MASTER-92 — Red Team Regression

URBION is attacked with invalid coordinates, unsupported PBT coverage, out-of-band TOD assumptions and failed shop-frontage evidence.

Expected guardrails:
- invalid input is rejected;
- unsupported local rules remain `REQUIRES REVIEW`;
- an out-of-band TOD scheme becomes `NOT APPLICABLE`;
- failed verified controls remain `NON-COMPLIANCE`.

The goal is to prove the decision layer does not manufacture certainty when inputs are hostile or incomplete.
