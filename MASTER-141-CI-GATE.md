# MASTER-141 — CI Gate

The championship-loop checkpoint is valid only after the repository CI workflow passes on the resulting main commit.

Required contract: `tests/test_championship_loop_contract.py`.

Passing CI protects the documented product-first operating loop and statutory/evidence boundary from accidental regression.
